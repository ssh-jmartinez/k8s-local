apiVersion: v1
kind: Service
metadata:
  name: landing-page-service
  labels:
    app: landing-page
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  selector:
    app: landing-page